[algokit]
min_version = "v2.0.0"

[project]
type = "frontend"
name = 'algorand-agent-pass-frontend'
artifacts = "src/contracts"

[project.run]
build = { commands = ['pnpm run build'], description = 'Build frontend' }
test = { commands = ['pnpm run test'], description = 'Run frontend tests' }
